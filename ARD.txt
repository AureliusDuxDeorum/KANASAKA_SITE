KANASAKA SITE — Architecture Reference Document (ARD)
======================================================
Maintained by: Cursor agent (update this file after every substantive change)
Last updated: 2026-08-01
Repo: AureliusDuxDeorum/KANASAKA_SITE
Local path: ~/KANASAKA_SITE
Production: https://kanasaka.com


1. PURPOSE
----------
Static marketing/site shell with gated downloads, contact details, and email-based
account auth. Backend is Cloudflare Pages Functions + D1. No separate VPS.


2. RUNTIME STACK
----------------
Hosting       Cloudflare Pages (branch: main → production)
Functions     Cloudflare Pages Functions under /functions
Database      Cloudflare D1 — kanasaka-auth (id: b76e77d3-4056-4ba2-8356-c69fb65a13d9)
Email         Resend API (FROM: noreply@kanasaka.com)
DNS/TLS       Cloudflare
Rate limiting Cloudflare WAF (see docs/CLOUDFLARE_WAF.md); app-level limits removed
Build         No npm deps required for deploy; Argon2 WASM vendored in functions/vendor/


3. REPO LAYOUT (KEY PATHS)
--------------------------
functions/lib/auth.js       Core auth: sessions, tokens, passwords, users
functions/lib/argon2.js     Argon2id hashing (imports vendored WASM)
functions/lib/tokens.js     Raw token generation + HMAC-SHA256 secret hashing
functions/lib/schema.js     Runtime D1 schema detection (legacy vs token_hash tables)
functions/lib/security.js   Origin checks, auth event logging
functions/lib/email.js      Verification + reset emails via Resend
functions/lib/profile.js    Display name + avatar BLOB storage
functions/api/auth/*        Register, login, logout, verify, reset, session
functions/api/account/*     Profile, password, avatar, delete
functions/api/contact.js    Gated contact info (logged-in only)
functions/api/download/     Gated signed downloads (R2 stream via /api/download/file)
functions/lib/downloads.js  HMAC-signed download tokens + R2 object keys
functions/lib/totp.js       RFC 6238 TOTP (Web Crypto HMAC-SHA1)
functions/lib/two-factor.js 2FA setup/enable/disable + login challenges
functions/vendor/argon2-wasm-edge/  Vendored Argon2 lib + WASM (no npm install on CI)
migrations/002–008.sql      D1 schema migrations (run manually on remote)
wrangler.toml               Pages + D1 + R2 binding config
scripts/pages-build.mjs     No-op build step (Cloudflare Pages)
scripts/generate_pages.py   Static HTML page generator
scripts/check-waf-rules.mjs WAF audit vs docs/CLOUDFLARE_WAF.md
scripts/upload-installers-r2.mjs  Upload KS Unify installers to R2
docs/R2_INSTALLERS.md       R2 bucket setup + upload instructions
docs/WAF_STATUS.txt         Last WAF audit output (scripts/check-waf-rules.mjs)
docs/CLOUDFLARE_WAF.md      WAF rule documentation


4. AUTH DESIGN
--------------
Password policy (register, reset, change password)
  Min 12 chars, max 128
  Requires uppercase, lowercase, digit, special character
  No spaces; no more than 2 identical characters in a row
  Blocklist of common passwords (incl. kanasaka, ksunify)
  Must not contain email local-part (before @)
  Login does NOT re-apply policy (existing shorter passwords still work until changed)
  Server: functions/lib/password-policy.js
  Client: assets/js/password-policy.js (keep in sync)

Registration
  POST /api/auth/register
  Email + password (policy above). Password hashed Argon2id.
  Creates user (email_verified=0), email verify token, sends Resend email.
  Anti-enumeration: existing verified email → generic success; unverified → resend.

Email verification
  POST /api/auth/verify — consumes one-time token from email link
  GET verify page on site loads token from URL query

Login / session
  POST /api/auth/login — cookie __Host-kanasaka_session (HttpOnly, SameSite=Strict)
  If totp_enabled: returns { twoFactorRequired, challenge, expiresIn } (no cookie yet)
  POST /api/auth/two-factor — completes login with TOTP or backup code
  Session token: 32-byte random, only HMAC digest stored in D1 (needs SESSION_SECRET)
  Session rotation: every SESSION_ROTATE_HOURS (default 24h) via resolveSession()
  GET /api/auth/session — current user payload

Two-factor authentication (TOTP)
  Optional per account; RFC 6238 via functions/lib/totp.js
  Secrets encrypted at rest (AES-GCM, key derived from SESSION_SECRET)
  POST /api/account/two-factor/setup — pending secret + otpauth URL
  POST /api/account/two-factor/enable — confirm code, returns backup codes
  POST /api/account/two-factor/disable — password + TOTP required; clears sessions
  Backup codes: one-time, stored hashed in twofa_backup_codes
  UI: login 2FA step + Account Settings → Two-Factor Authentication panel

Password reset
  POST /api/auth/forgot-password → email with reset link
  POST /api/auth/reset-password → new password, invalidates all sessions

Account
  /api/account/profile, /password, /avatar, /delete

Security
  requireSameOrigin() on mutating auth routes
  logAuthEvent() → auth_events table (best-effort)
  Legacy PBKDF2 hashes still verify; upgraded to Argon2 on login


5. D1 SCHEMA (CURRENT — post migration 008)
-------------------------------------------
users
  id INTEGER PK, email TEXT UNIQUE NOCASE, password_hash TEXT,
  email_verified INTEGER, created_at TEXT, display_name TEXT,
  totp_secret TEXT, totp_pending_secret TEXT,
  totp_enabled INTEGER DEFAULT 0, totp_enabled_at TEXT

sessions
  id INTEGER PK, token_hash TEXT UNIQUE, user_id INTEGER FK,
  expires_at TEXT, created_at TEXT, last_rotated_at TEXT

email_tokens
  id INTEGER PK, token_hash TEXT UNIQUE, user_id INTEGER FK,
  type TEXT ('verify'|'reset'), expires_at TEXT, created_at TEXT

user_avatars
  user_id INTEGER PK FK, mime_type TEXT, data BLOB, updated_at TEXT

auth_events
  id INTEGER PK, event_type TEXT, meta TEXT JSON, created_at TEXT

twofa_backup_codes
  id INTEGER PK, user_id INTEGER FK, code_hash TEXT, used_at TEXT, created_at TEXT

twofa_challenges
  id INTEGER PK, challenge_hash TEXT UNIQUE, user_id INTEGER FK,
  expires_at TEXT, created_at TEXT

rate_limits
  key TEXT PK, count INTEGER, window_start TEXT
  (legacy table; app no longer writes — WAF handles rate limits)

schema.js detects token_hash vs legacy id-based token tables at runtime.


6. SECRETS & BINDINGS (Cloudflare Pages → Production)
-----------------------------------------------------
Required
  DB              D1 binding → kanasaka-auth
  SESSION_SECRET  HMAC pepper for session/email tokens (32+ random bytes)
  RESEND_API_KEY  Transactional email
  INSTALLERS      R2 binding → kanasaka-installers (signed download streaming)

Optional
  ADMIN_SECRET    Bearer token for /api/auth/email-status
  ARGON2_MEMORY_KIB, ARGON2_ITERATIONS, ARGON2_PARALLELISM
  SESSION_ROTATE_HOURS, SESSION_DAYS (also in wrangler.toml [vars])

Public vars (wrangler.toml)
  SITE_URL, FROM_EMAIL, FROM_NAME, SESSION_DAYS
  ARGON2_MEMORY_KIB=19456, ARGON2_ITERATIONS=3 (Workers Paid defaults)


7. MIGRATIONS (RUN FROM ~/KANASAKA_SITE)
----------------------------------------
  npx wrangler d1 execute kanasaka-auth --remote --file=./migrations/003_profile.sql
  npx wrangler d1 execute kanasaka-auth --remote --file=./migrations/004_security.sql
  npx wrangler d1 execute kanasaka-auth --remote --file=./migrations/005_auth_hardening.sql
  npx wrangler d1 execute kanasaka-auth --remote --file=./migrations/006_repair_token_schema.sql
  npx wrangler d1 execute kanasaka-auth --remote --file=./migrations/007_wipe_accounts.sql
  npx wrangler d1 execute kanasaka-auth --remote --file=./migrations/008_security_hardening.sql
  npx wrangler d1 execute kanasaka-auth --remote --file=./migrations/010_tos_acceptance.sql

007_wipe_accounts.sql — IRREVERSIBLE data wipe (users, sessions, tokens, avatars,
  auth_events, rate_limits). Schema preserved.

008_security_hardening.sql — TOTP columns on users, twofa_backup_codes,
  twofa_challenges tables.


8. DEPLOY / BUILD
-----------------
Git: main → Cloudflare Pages production
PR #18 merged 2026-08-01 (registration fix + vendored Argon2)

Cloudflare Pages build command (dashboard): currently EMPTY — OK because Argon2 is
vendored; Functions bundle without npm ci.

Do NOT add [build] to wrangler.toml — Pages rejects it.

Optional dashboard build command if npm deps return: npm ci && npm run build

GitHub PR checks: Cloudflare Pages must pass for merge (was failing before c1417b3).


9. DOWNLOADS
------------
Gated behind login at /downloads/
GET /api/download/{platform} — auth check → 302 to signed /api/download/file?token=...
GET /api/download/file — validates HMAC token (5 min TTL), streams object from R2
Platforms: windows (.exe), linux (.deb), macos (.dmg)
R2 bucket: kanasaka-installers (enable R2 in dashboard, bind INSTALLERS in Pages)
Upload: node scripts/upload-installers-r2.mjs (see docs/R2_INSTALLERS.md)
Legacy GitHub URLs removed from download flow (DOWNLOAD_URLS may remain in auth.js)
KS Unify desktop app (separate repo) uses cookie-jar auth client — no localStorage tokens.


10. OPERATIONAL NOTES
---------------------
- Always run D1 commands from ~/KANASAKA_SITE, not KS_UNIFY
- Production deploy can lag if Cloudflare build fails; check Deployments dashboard
- Registration SQLITE_MISMATCH was caused by old code + new D1 schema mismatch
- Remote D1 wiped 2026-08-01: 0 users, clean slate for testing


11. CHANGE LOG (agent-maintained)
---------------------------------
2026-08-01  Security hardening: TOTP 2FA, R2 signed downloads, Argon2 bump (19 MiB / 3 iter).
2026-08-01  Migration 008 applied on remote D1 (2FA schema).
2026-08-01  WAF audit script added; API token lacks ruleset scope — verify manually in dashboard.
2026-08-01  Added full password policy (12+ chars, classes, blocklist, live UI checklist).
2026-08-01  ARD created. Documented full auth stack, D1 schema, deploy model.
2026-08-01  PR #18 merged: schema detection, D1 bind fixes, vendored Argon2 WASM.
2026-08-01  Remote D1 wiped via 007_wipe_accounts.sql (3 users removed).
2026-08-01  Cloudflare build fix: vendor argon2 under functions/vendor/ (c1417b3).
2026-08-01  Identified root cause: empty Pages build command + npm-only argon2 imports.
